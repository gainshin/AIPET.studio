# 這個 repo 是怎麼長出來的

給不想看 `git log` 技術寫法的人：每一次修改在做什麼、為什麼做，
用白話寫一遍。時間由舊到新，由上往下讀就是完整的建造過程。

想查某次修改的完整技術細節，到 [GitHub 的 commits 頁](https://github.com/gainshin/AIPET.studio/commits/Claude)
用同一天的日期對照即可。

---

## 起點：一個 Create React App 樣板（2025-08）

專案最早是一個標準的 React 網頁樣板，還沒有現在的核心概念。
這幾次修改只是把最初的頁面架好、修好一個排版工具（Tailwind CSS）的
設定錯誤、拿掉語言切換按鈕上不需要的小旗子圖示。

## 打地基：六個基礎建設（2026-07-10）

這一批把專案從「一個網頁」變成「一套有規則的系統」。

- **清掉不相關的東西**：砍掉舊樣板裡跟這個專案無關的頁面和程式碼，只留下
  多語言切換和一個說明理論的頁面。
- **換掉打包工具**：把網頁的建置方式換成更快的工具（Vite），純粹是
  工程效率的事，使用者不會有感覺。
- **寫下六份「規格書」**：用機器看得懂的格式（JSON Schema），定義
  「一次修改長什麼樣」「一個 AI 角色的授權書長什麼樣」等六種基本規則。
  之後任何違反規則的東西都會被自動擋下來，不用靠人工檢查。
- **建立「設計系統的說明書」**：一份文件（DESIGN.md）規定顏色、字體、
  間距怎麼用；並做了一個「翻譯層」，讓不同的視覺主題可以共用同一套規則。
- **寫下五份「AI 角色的工作說明書」**：規定每個 AI 角色（畫介面的、
  做研究的、寫文案的、找碴審查的，加上人類自己）能做什麼、不能做什麼。
- **記錄兩個重要決定**：為什麼要有「人類在中間管全局」這個設計、
  以及整個系統的三層架構長什麼樣。

## 設計系統上色：品牌識別與樣式說明頁（2026-07-11）

- **設計出一套完整的視覺主題**：深綠底色配奶油色文字、琥珀色點綴——
  參考一份內部設計素材，重新整理成這個專案自己的品牌樣子。
- **新增一個「樣式說明頁」**：把顏色、字體、間距全部整理成一頁可以
  直接查看的文件，而且是自動從規格書生成的——改規格、頁面就跟著變，
  不用兩邊手動同步。
- **改成左右兩欄排版**，並確保手機螢幕看起來也正常。
- **加入圖表色彩的細部規則**：包含色盲友善的色彩驗證，以及圖表要怎麼
  畫才清楚（甘特圖、關聯圖、分級表格）。
- **修掉可讀性問題**：字太小、顏色選得不好看、線條看不清楚、
  手機版出現橫向捲軸——這些都個別修正。
- **做出兩個可以雙擊打開的示範網頁**：一個是「觀察者」（只能看、
  不能改，用來檢查一個修改的來龍去脈）；一個是「編輯者」
  （可以提出修改建議，但要通過審查才會生效）。

## 新增「已完成項目」頁面（2026-07-20）

在網站上加一個導覽分頁，列出目前做好的設計主題與兩個示範網頁的連結，
方便直接點進去看。

## 把「編輯者」示範網頁做得更真實（2026-07-23 上午）

- **改成更接近真實 AI 協作的流程**：使用者說出想改什麼、AI 一步步
  展示它的思考過程、提出兩個不同的修改方案、另一個 AI 角色負責挑毛病，
  最後由人類選一個——不是 AI 自己說了算。
- **寫下一條核心原則並記錄成正式文件**：這個系統不做「打一句話就生成
  整個畫面」的事，畫面永遠要能追溯回一份文件；追不回來的畫面，
  這裡直接稱作「垃圾產出」。

## 把說明文件全部重寫（2026-07-23 下午，四次）

專案的說明文件（README）前後改了四個版本，一次比一次更貼近真實讀者：

1. 先加入教學式的操作步驟和「不同背景的人該從哪裡開始讀」的建議。
2. 再改寫成「每一句話都能在專案裡找到證據」的嚴謹格式——像一份
   合約，不是行銷文案。
3. 後來發現這樣寫太技術了，一般用設計工具的人看不懂，
   **改成三條完全不同的路線**：只用 Figma／ChatGPT／Canva 的人、
   帶團隊的 UX 主管、自己做產品的 AI PM——各自從自己熟悉的東西講起。
4. 最後修掉幾個連結失效的問題（見下面「整理公開範圍」）。

## 給想直接開新專案的人：一份現成的資料夾模板（2026-07-23）

做了一整套**空白模板**，任何人都可以複製這個資料夾去開始自己的專案——
裡面已經放好競品分析、使用者訪談、決策記錄等 22 份文件範本，
不需要懂程式，用一般文字編輯器就能填寫。

## 整理公開範圍：哪些東西不該放在公開的 repo 上（2026-07-23）

- **把內部規劃文件擋在外面**：專案的內部規劃書、工作守則、決策記錄
  這類文件只給團隊內部用，設定成不會被推上公開的 GitHub。
- **拿掉一批內部課程的範例資料**，換成一份全新、乾淨、不含任何
  公司內部資訊的示範資料，讓圖表範例對任何人都能直接執行、不會出錯。
- **修好因為上述整理而壞掉的連結**，並把設計文件裡殘留的
  公司名稱、內部素材檔名都改成中性說法，不透露是哪家公司或哪份文件。

---

**小結**：這個 repo 從一個普通網頁樣板開始，先建好機器可以檢查的規則，
再疊上看得見的設計，最後把「怎麼跟不同的人解釋這件事」磨了四遍——
現在任何人都能照著自己熟悉的方式，找到自己的起點。
